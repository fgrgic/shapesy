import React from "react";
import { View, Text } from "react-native";

interface Props {}

const Game = (props: Props) => {
  return (
    <View>
      <Text>Game</Text>
    </View>
  );
};

export default Game;
